# BGP 201 - Ödev 3 (TypeScript)

## Teslim Bilgileri
- **Öğrenci:** Ahmet Baran Özkerim 240408487  
- **Repo:** https://github.com/BaranOzkerim/BGP201-Odev3  
- **Teslim Tarihi:** 7 Kasım 2025  

---

## Sorular ve Açıklamalar

| Soru | Açıklama |
|------|---------|
| **Soru 1** | `mergeArrays` jenerik fonksiyonu ile iki farklı tipte diziyi birleştirme |
| **Soru 2** | `in` operatörü ile tip koruyucusu (type guard) kullanarak `Car` ve `Truck` ayrımı |
| **Soru 3** | `private logHistory` ile dışarıdan erişilemeyen log sistemi |
| **Soru 4** | `keyof` ve jenerik ile `getProperty` fonksiyonu |
| **Soru 5** | Fonksiyon aşırı yüklemesi ile ID veya isimle kullanıcı arama |
| **Soru 6** | Jenerik `MemoryCache` sınıfı (`set`, `get`, `clear`) |
| **Soru 7** | `Partial<User>` ile kullanıcı güncelleme, `Readonly<User>` döndürme |
| **Soru 8** | Rest parametreleri ile `sum` fonksiyonu |
| **Soru 9** | `abstract class Shape` ve `Circle` ile alan hesaplama |
| **Soru 10** | `static PI` ve `calculateCircumference` metodu |
| **Soru 11** | `infer` ile `Promise` açma (`UnwrapPromise`) |
| **Soru 12** | `infer` ile `ReturnType` benzeri `GetReturnType` |
| **Soru 13** | Mapped Types + Template Literals ile `getId`, `getName` dönüşümü |
| **Soru 14** | Rekürsif `DeepReadonly` tipi |
| **Soru 15** | `PickByValueType` ile tip bazlı filtreleme |
| **Soru 16** | Branded Types ile `UserID` ve `PostID` ayrımı |
| **Soru 17** | Dağıtılmış koşullu tipler ile `FilterUnion` |
| **Soru 18** | `infer` ile fonksiyonun son parametresi |
| **Soru 19** | `infer` ile dizi elemanı tipi (`Flatten`) |
| **Soru 20** | Rekürsif Template Literal Parsing ile route parametreleri |

---

## Not
Tüm sorular `Soru1.ts` → `Soru20.ts` dosyalarında, **çalışır halde** ve **beklenen çıktıyı verir**.
